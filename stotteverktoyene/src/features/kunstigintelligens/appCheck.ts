// NOTE: App Check should be initialized once, as early as possible (in main.tsx).
// We keep the canonical initialization in `src/firebase/appCheck.ts`.
// This file exists only as a convenience import location for the AI feature area.

import "../../firebase/appCheck";

export {};
